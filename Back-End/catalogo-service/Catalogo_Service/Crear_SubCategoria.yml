info:
  name: Crear_SubCategoria
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8082/v1/catalogo/subCategoria
  body:
    type: json
    data: |-
      {
        "nombre":"nueva sub",
        "idCategoria":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
